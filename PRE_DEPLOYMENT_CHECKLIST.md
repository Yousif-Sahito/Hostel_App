# 📋 Pre-Deployment Checklist\n\n## Backend Configuration\n\n### .env File Setup\n- [ ] Copy `backend/.env.example` to `backend/.env`\n- [ ] Set `DATABASE_URL` with your MySQL connection\n- [ ] Generate and set strong `JWT_SECRET` (e.g., `openssl rand -base64 32`)\n- [ ] Set `ADMIN_EMAIL` (optional, default: admin@hostel.com)\n- [ ] Set strong `ADMIN_PASSWORD` (change from default!)\n- [ ] Set `NODE_ENV` (development/production)\n- [ ] Optional: Add Firebase `SERVICE_ACCOUNT_KEY` for notifications\n- [ ] **IMPORTANT:** Add `.env` to `.gitignore`\n\n### Database Setup\n- [ ] MySQL server running and accessible\n- [ ] Database created and accessible via `DATABASE_URL`\n- [ ] Run `npm run prisma:push` to create tables\n- [ ] Run `npm run seed:admin` to create admin user\n- [ ] Run `npm run seed:settings` to create hostel settings\n- [ ] Verify tables exist: `User`, `Bill`, `Payment`, `MealEntry`, etc.\n\n### Dependencies Installation\n- [ ] Run `npm install` in backend directory\n- [ ] Verify no install errors\n- [ ] Check `node_modules` exists\n\n### Server Testing\n- [ ] Run `npm run dev` successfully\n- [ ] Server prints \"Server running on port 5000\"\n- [ ] Verify \"Database connected successfully\"\n- [ ] Hit `http://localhost:5000` and get JSON response\n- [ ] Stop server with Ctrl+C\n\n---\n\n## Frontend Configuration\n\n### Base URL Configuration\n- [ ] Open `lib/services/auth_service.dart`\n- [ ] Update `_baseUrl` for your environment:\n  - [ ] Android Emulator: `http://10.0.2.2:5000/api`\n  - [ ] iOS Simulator: `http://localhost:5000/api`\n  - [ ] Physical Device: `http://YOUR_SERVER_IP:5000/api`\n\n### Dependencies Installation  \n- [ ] Run `flutter pub get` in frontend directory\n- [ ] Verify no dependency errors\n- [ ] Check iOS/Android build configuration\n\n### Firebase Setup (Optional - for Notifications)\n- [ ] Create Firebase project in Firebase Console\n- [ ] Download Google services configuration:\n  - [ ] `google-services.json` for Android → `android/app/`\n  - [ ] `GoogleService-Info.plist` for iOS → `ios/Runner/`\n- [ ] Add Firebase service account key to backend `.env`\n- [ ] Run `flutter pub get` again after file changes\n\n### Build & Test\n- [ ] Run `flutter run` successfully\n- [ ] App launches without errors\n- [ ] Check debug console for \"✅ FCM Service initialized\"\n- [ ] Login works with test credentials\n- [ ] First screen loads correctly\n\n---\n\n## Integration Testing\n\n### Backend Running\n- [ ] Terminal 1: Server started with `npm run dev`\n- [ ] Terminal shows \"Server running on port 5000\"\n- [ ] Database connection successful\n\n### Frontend Running  \n- [ ] Terminal 2: App running with `flutter run`\n- [ ] App displays login screen\n- [ ] Console shows FCM Token\n- [ ] Can interact with UI\n\n### Basic Authentication\n- [ ] Login with admin credentials works\n- [ ] JWT token received and stored\n- [ ] Dashboard loads after login\n- [ ] Logout works\n- [ ] Tokens cleared after logout\n\n### FCM Token Sync\n- [ ] App logs \"✅ FCM token updated successfully on backend\"\n- [ ] Token is consistent across app restarts\n- [ ] Backend `.env` contains valid `FIREBASE_SERVICE_ACCOUNT_KEY` (if notifications enabled)\n\n### API Communication\n- [ ] Backend console shows incoming requests\n- [ ] Frontend receives successful responses\n- [ ] Error responses handled gracefully\n- [ ] Network errors display user-friendly messages\n\n---\n\n## Database Verification\n\n### Tables Created\n```sql\nSHOW TABLES; -- Should show:\nSELECT * FROM \\`User\\` LIMIT 1;\nSELECT * FROM \\`HostelSetting\\` LIMIT 1;\nSELECT * FROM \\`Bill\\` LIMIT 1;\nSELECT * FROM \\`Payment\\` LIMIT 1;\nSELECT * FROM \\`MealEntry\\` LIMIT 1;\nSELECT * FROM \\`MessOffPeriod\\` LIMIT 1;\n```\n\n### Admin User Created  \n- [ ] Admin user exists in database\n- [ ] Email: matches your `ADMIN_EMAIL`\n- [ ] Role: `ADMIN`\n- [ ] Status: `ACTIVE`\n\n### Settings Initialized\n- [ ] Default hostel settings exist\n- [ ] Meal prices configured\n- [ ] Mess status set to ON\n\n---\n\n## Security Verification\n\n### Secrets\n- [ ] `JWT_SECRET` is NOT the default placeholder\n- [ ] `ADMIN_PASSWORD` is NOT \"admin123\"\n- [ ] No secrets logged to console in production\n- [ ] `.env` file NOT committed to git\n\n### API Security\n- [ ] All protected routes require valid JWT token\n- [ ] Invalid tokens rejected with 401 error\n- [ ] Admin routes reject non-admin users with 403 error  \n- [ ] Input validation working (test with invalid data)\n\n### HTTP Security\n- [ ] Helmet middleware active (check response headers)\n- [ ] CORS properly configured for your domain\n- [ ] No sensitive data in error messages\n\n---\n\n## Production Deployment\n\n### Before Going Live\n- [ ] All tests passing\n- [ ] No console errors or warnings\n- [ ] Performance acceptable\n- [ ] Database backups configured\n- [ ] Error tracking setup (Sentry, etc.)\n- [ ] Monitoring setup (uptime, logs)\n- [ ] HTTPS certificate obtained\n- [ ] Domain configured\n- [ ] Email notifications setup (optional)\n\n### Production Environment\n- [ ] `NODE_ENV=production`\n- [ ] Strong, unique `JWT_SECRET`\n- [ ] Production database URL\n- [ ] Firebase configured for production\n- [ ] Flutter app updated with production server URL\n- [ ] App built in release mode\n- [ ] Signed APK/IPA ready\n\n### Post-Deployment\n- [ ] Monitor backend logs for errors\n- [ ] Monitor database performance\n- [ ] Check Firebase notification delivery rates\n- [ ] Verify user reports no issues\n- [ ] Keep backups of database\n\n---\n\n## Common Issues & Troubleshooting\n\n### Backend Won't Start\n- [ ] Check `DATABASE_URL` is valid\n- [ ] Verify MySQL server is running\n- [ ] Check port 5000 is not in use\n- [ ] Review error message in console\n\n### Flutter App Won't Connect\n- [ ] Verify backend is running on correct port\n- [ ] Check `_baseUrl` in `auth_service.dart`\n- [ ] On emulator: Use `10.0.2.2` not `localhost`\n- [ ] Check firewall allows port 5000\n- [ ] Verify device/emulator can ping server\n\n### FCM Token Not Updating\n- [ ] Check app has notification permissions\n- [ ] Verify JWT token is valid\n- [ ] Check backend logs for errors\n- [ ] Verify Firebase configured (if notifications enabled)\n\n### Notifications Not Received\n- [ ] Verify Firebase configured correctly\n- [ ] Check `FIREBASE_SERVICE_ACCOUNT_KEY` in .env\n- [ ] Verify app has notification permissions\n- [ ] Check device is online\n- [ ] Review Firebase console for failures\n\n---\n\n## Sign-Off\n\n- [ ] All checklist items completed\n- [ ] System tested end-to-end\n- [ ] Ready for production deployment\n- [ ] Team notified and trained\n- [ ] Documentation updated\n- [ ] Backup strategy in place\n\n**Date Completed:** _______________\n**Completed By:** _______________\n**Notes:** \n```\n\n\n\n```\n\n---\n\n✅ **System is production-ready!**\n